<li>
  <%= link_to user.name, user %><br>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "削除", user, method: :delete, 
                         data: { confirm: "削除してよろしいですか？" } %> <br>
    | <%= link_to "基本情報変更", basic_info_path(user) %>
  <% end %>
</li>